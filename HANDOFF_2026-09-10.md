# 태국어유니버스 — 세션 인수인계 (2026-09-10)

> 새 Claude Code 세션에서 이 파일을 먼저 읽고 이어서 작업. 이전 세션이 Remote Control 403(만료된 세션 등록)으로 막혀 새 세션을 연다.
> 앱: Flutter `C:\Users\Johnjeon\talkverse\th` · GitHub `talkverselab/talkverse-th` (master) · 패키지 `com.talkverse.thai_universe` · 폰 S25 `R3CY20HDN2K`

## 0. 현재 상태 (모두 완료·푸시·설치됨)
- 최신 커밋 **`34958c0`** master, origin과 동일. (S25는 미연결 — 설치는 CI APK 또는 케이블 연결 후)
- `flutter analyze` 이슈 0, `flutter test` 20개 통과.
- 남은 작업 없음. 새 요청부터 진행.

## 1. 항상 지킬 규칙 (사용자 확정 취향)
- 독음은 **한글만** (로마자 금지; 로마자 매뉴얼·성조 로마자 모드만 예외). 이탤릭 안 씀.
- 디자인 zh 스타일 플랫: 크림 배경, 직각 타일, 얇은 골드 테두리, 그라데이션·글로우·마스코트 금지.
- **출처 노출 금지**: 책명·쪽수·원서 표기·참고 P.·Netflix 언급을 앱·리포·에셋·코드 어디에도 남기지 않음. 원서 문장·자막 코퍼스 원문은 앱에 넣지 않음(단어만, 문장은 자체 제작).
- 대화는 8턴, 연애·플러팅 등 재밌는 상황. 남성 화자 남성 음성, 여성 화자 여성 음성, 그 외 단어·표현은 여성 음성, 재생 1초 지연.
- 빈도 1단계=최상위. 플래시카드 기본 한→태. 고유명사·ครับ/ค่ะ 밑줄 없음.
- 사용자는 한 턴에 여러 요청을 연달아 보냄 → 목록으로 전부 처리. 폰 연결돼 있으면 묻지 말고 설치. 요청이 "푸시/설치"면 commit → push → `flutter build apk --release` → adb install → 앱 실행까지.
- 커밋 트레일러: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` (세션 트레일러는 새 세션 것으로).

## 2. 반복 절차
```
# 푸시 (gh 활성 계정이 다른 계정으로 자주 바뀜 → 403 시 아래 먼저)
gh auth switch --user talkverselab
git push origin master

# 빌드·설치 (백그라운드로 돌리고 완료 알림 후 확인)
ADB="C:/Users/Johnjeon/AppData/Local/Android/Sdk/platform-tools/adb.exe"
flutter build apk --release
"$ADB" -s R3CY20HDN2K install -r build/app/outputs/flutter-apk/app-release.apk
"$ADB" -s R3CY20HDN2K shell "input keyevent KEYCODE_WAKEUP; wm dismiss-keyguard; monkey -p com.talkverse.thai_universe -c android.intent.category.LAUNCHER 1"
```
- adb에 기기가 안 잡히면 사용자에게 케이블/USB 모드(파일 전송)/디버깅 허용 확인 요청. 잠금(Bouncer) 상태면 스크린샷 불가.
- 폰이 다른 앱에 포커스돼 있으면 스크린샷 시도 중단.
- CI: push → GitHub Actions → `releases/latest` `thai-universe-latest.apk` 갱신.

## 3. 에셋 생성 순서 (태국어 데이터 바꿀 때, 모두 `python -X utf8`)
1. `tools/vocab_pipeline/build_spoken_freq.py` → `assets/data/wordsets/th_top1000.csv` (로컬 `corpus/analysis_th/_pairs_clean.jsonl` 자막 통계; 새 단어 뜻은 `spoken_meanings_N.json`)
2. `build_phrasebook.py` → `phrasebook_parsed.json` (로컬 전용, gitignore)
3. `build_vocab.py` → `th_vocab.json`(topic/part/rank/level), `th_topics.json`, `th_expressions.json`, `th_roots.json`(`roots_curated.json` + `root_pieces.json` 병합)
4. `build_obec.py` → `assets/data/wordsets/th_obec_basic.json`
5. `tools/tone_pipeline/build_tones.py` → `th_tones.json` (`pip install pythainlp python-crfsuite`)
6. `flutter test`
- `status.py`로 체크포인트 누락 확인. 원서 전사(`pages/*.md`, `book/*.md`)는 로컬 전용.

## 4. 이번 세션(2026-09-09)에 바뀐 것
### 단어장 메뉴 (`VocabHubScreen`)
- 진입 시 두 카드: **📚 주제별 단어**(`TopicVocabScreen`, 13주제) / **⭐ 중요 단어**(`ImportantWordsScreen`).
- 중요 단어 = 카드 2개: **중요 1,000단어**(회화 빈도 937, 안에서 칩 「전체 / 1단계·1~500위 / 2단계·501~1000위」) + **표준 단어**(교육부 기초 중 1000위 밖 2,115). 합계 3,052 = 앱 최대 범위. 아래 「전체 한눈에」 버튼.
- `TopicWordsScreen`에 `entries:`(목록 직접 지정), `emojiTopic:`, `partOf:`(칩 이름 계산) 옵션 추가. 타일 비율 0.66, 뜻 글자 13.5/행간 1.2 (세로 넘침 수정).
- 카드 글자 넘침 수정: 배지 FittedBox, 제목/범위 줄 분리, 말줄임.

### 루트 단어 (`ThaiRootsScreen`, `RootService`)
- 상단 버튼 **「중요 1,000단어」**(`RootStage.upTo5`) / **「표준 단어」**(`RootStage.standard`, 빈도1000+OBEC). 전체(4,558) 버튼 제거. enum에 upTo2~4 남아 있으나 UI 미사용.
- 가족 타일에 분해 공식 `RootService.formula` → 「방 + 물 ⇒ 화장실」. 뜻 없는 조각은 **「어원불명확」** (`RootService.unknownLabel`).
- 후보(weak) 섹션 폐지. `tools/vocab_pipeline/root_pieces.json`: `pieces`(조각 뜻 211) + `reject`(우연 일치 140, `"root|word"`) → `th_roots.json`의 `pieces`/`reject`. 파편(자음 하나·부호 시작·괄호)은 `isFragment`로 자동 제외. 표준 범위 가족 635개 중 어원불명확 31개.
- `VocabService`: `entriesUpToLevel(n)`, `standardEntries`(빈도1000+OBEC 중복 제거), `standardExtraEntries`(OBEC 중 level 0).

### 단어 그림 (`EmojiService`, `assets/data/vocab/th_emoji_map.json`)
- `words`: **태국어 표제어 → 그림** 476개(1단계 1~500위 전부 지정, 최우선). `keywords` 1,264(3자 이하 키워드는 낱말 경계 매칭 — '만약'의 '약'💊 같은 오매칭 수정). 그 다음 주제 풀. 한 화면 내 중복 없음(1단계 477 중 52개는 풀로 대체).
- 그림 바꾸려면 `words`에 표제어 키로 추가. `assign(kos, ths:, topic:)`.
- 테스트: 1단계 단어 그림 누락 검사(`data_assets_test`).
- **2단계(501~1000위) 460개도 표제어별 지정 완료**(2026-09-10) — `words` 937개 = 1·2단계 전부. 마음에 안 드는 그림은 `words`의 표제어 값만 바꾸면 됨.
- `assign()`은 **표제어 직접 지정 그림을 겹쳐도 그대로** 쓰고, 키워드·풀 배정만 화면 안 중복을 피한다(전체 937 화면에서 검토된 그림이 풀 그림으로 밀려나지 않게).

## 5. 이전 세션 누적 구조 (참고)
- 메인: 퀵메뉴(⚡바로 문장·📖단어장·🗣️표현학습·💬회화·🎙️문장 말하기·⌨️키보드연습·🔍청크 검색·🎴복습) + 타일(문자 44·성조 5·문법·단어장·표현학습·Manual for English·영어 유래 단어·루트 단어…).
- Drift DB는 대화 turns만, 시드 키 `_kSeededKey = 'db_seeded_th_v5'`(`lib/data/db/seed_loader.dart`) — 대화 수정 시 +1. 대화 L1(7편)/L2(연애 챗 6편)/L3(여행 18편) 모두 8턴.
- 말하기 연습 7/4/2초, `speech_to_text` th_TH, 단어 커버리지 판정(관대), 힌트 첫 어절 토글.
- 성조 DB `th_tones.json`, 독음 4단계 토글(한/한+성조/로마자+성조/숨김).
- OBEC 표준 DB 2,625(뜻 완비) — 시험 공식 어휘표는 없음.
- 빈도 Top1000은 **로컬 자막 코퍼스 통계**(문서 코퍼스 아님).
- 메모리 파일: `~/.claude/projects/C--Users-Johnjeon-talkverse-th/memory/` (`talkverse-th-project-state.md`, `user-preferences-th-app.md`, `vocab-pipeline-resume.md`, `MEMORY.md`) — 최신 상태 반영됨.

## 6. 선택 후속 (사용자가 요청하지 않음)
- 자동 성조 오류 검토, 캡처 단어의 "회화 핵심어휘" 주제 재배치.
- 커밋 9248fe6에 원서 전사 `pages/*.md`가 남아 있음 → 필요 시 history rewrite(filter-repo).
- Remote Control: 계정·네트워크 정상, 별도 터미널 `claude remote-control`은 연결됨. 새 세션에서 `/remote-control` 재시도.
